/**
 * @file  drv_{{ peripheral.name }}.c
 * @brief Generic I2C pressure sensor driver implementation (generated by hw2c)
 *
 * Auto-generated — do not edit manually.
 */
{% set prs = peripheral.get('extra', {}) %}
{% set reg_start = prs.get('reg_start', model.get('extra_schema', {}).get('reg_start', {}).get('default', 0)) %}
{% set scale = prs.get('scale_kpa_per_lsb', model.get('extra_schema', {}).get('scale_kpa_per_lsb', {}).get('default', 0.001)) %}
{% set offset = prs.get('offset_kpa', model.get('extra_schema', {}).get('offset_kpa', {}).get('default', 0.0)) %}
{% set byte_order = prs.get('byte_order', model.get('extra_schema', {}).get('byte_order', {}).get('default', 'big')) %}

#include "drv_{{ peripheral.name }}.h"
#include <stddef.h>

int {{ peripheral.name }}_init(i2c_handle_t bus, uint8_t dev_addr)
{
    (void)dev_addr;
    if (bus == NULL) {
        return -1;
    }
    return 0;
}

int {{ peripheral.name }}_read(i2c_handle_t bus, uint8_t dev_addr,
                               {{ peripheral.name }}_data_t *out)
{
    uint8_t buf[3];

    if (bus == NULL || out == NULL) {
        return -1;
    }

    if (i2c_read_reg(bus, dev_addr, {{ reg_start }}, buf, 3, 0) != 0) {
        out->last_error = -1;
        return -1;
    }

{% if byte_order == 'little' %}
    out->raw = ((uint32_t)buf[2] << 16) | ((uint32_t)buf[1] << 8) | buf[0];
{% else %}
    out->raw = ((uint32_t)buf[0] << 16) | ((uint32_t)buf[1] << 8) | buf[2];
{% endif %}
    out->pressure_kpa = (float)out->raw * {{ scale }}f + {{ offset }}f;
    out->last_error = 0;
    return 0;
}
